interface:
  display_name: "Studio Use-Case Docs"
  short_description: "Capture AI workflows during coding work"
  default_prompt: "Use $studio-use-case-documenter to onboard once, capture workflow docs while implementing, and confirm writes with studio-agent."

policy:
  allow_implicit_invocation: true
